# Iot-securuty-system
This is an Iot security system that detects the object at the gate and calculate the distance between the object and the gate, then trigger the LEDs and alarm in accordance to distance. The system will display event details on the web dashboard for user interface.
